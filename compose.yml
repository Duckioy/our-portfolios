services:
  database:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=StrongP123
    ports:
      - "1433:1433"
    volumes:
      - database:/var/opt/mssql

volumes:
  database:

